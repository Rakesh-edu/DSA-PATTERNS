->solved a palindrome nnumber using math to string conversion
->it is a easy level problem with time complextiy of O(n)

code:
bool isPalindrome(int x) {
        string str=to_string(x);
        int start=0,end=str.size()-1;
        while(start<=end){
            if(str[start]!=str[end]) return false;

            start++;
            end--;
        }
        return true;