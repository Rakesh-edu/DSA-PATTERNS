//PREFIX sum
1. Add current number to sum
2. Check if (sum - k) existed before
3. Store current sum

//basic problem
int n=nums.size();
        vector<int>ans(n);
        ans[0]=nums[0];
        for(int i=1;i<n;i++){
            ans[i]=ans[i-1]+nums[i];
        }
        return ans;


//BASIC TEMPLATE
unordered_map<int,int> mp;
mp[0] = 1;

int sum = 0;
int count = 0;

for(int i = 0; i < n; i++) {
    sum += arr[i];

    if(mp.find(sum - k) != mp.end()) {
        count++;
    }

    mp[sum]++;
}

//Got a basic grip on prefix sum+hashmap
int subarraySum(vector<int>& nums, int k) {
        unordered_map<int,int>mp;
        int count=0,sum=0;
        mp[0]=1;
        for(int i=0;i<nums.size();i++){
            sum+=nums[i];
            if(mp.find(sum-k)!=mp.end()){
                count+=mp[sum-k];
            }
            mp[sum]++;
        }
        return count;
        
    }

    //solved today two leetcode questions from leetcode contest
    increment
    Master Trick

There are only 3 major prefix sum hashmap patterns:

1️⃣ Count subarrays → store frequency
2️⃣ Longest subarray → store first index
3️⃣ Modulo problems → store remainder